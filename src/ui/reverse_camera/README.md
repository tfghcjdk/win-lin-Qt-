# F08 倒车影像

点击首页入口后自动启动 Linux V4L2 摄像头，退出覆盖层时自动停止。实时画面按后视镜语义水平镜像，支持 MJPEG/YUYV，并叠加绿、黄、红三段固定辅助线；辅助线尺寸尚未按具体车型标定。

当前状态：已在 `src/ui/main_window.*`、`hmi_cards.*` 和 `paint_helpers.*` 实现对应 UI / 演示交互；真实设备与服务仍待接入。
