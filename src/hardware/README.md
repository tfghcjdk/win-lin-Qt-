# 硬件抽象与通信

SocketCAN 与 UART / 串口仍为预留接口。V4L2 摄像头已实现 MJPEG/YUYV、MMAP 缓冲、采集线程、断连/超时处理和安全释放；设备节点固定为 `/dev/video9`，优先请求 1280 × 720 / 30 FPS。`monocular_distance.*` 使用 OpenCV 3.4.16 对画面去畸变，在地面 ROI 中筛选障碍物轮廓，并根据内参、安装高度和俯角估算接地点距离。没有有效标定文件时测距保持禁用。Windows 后续通过平台摄像头后端接入。

当前状态：仅建立框架，尚未实现相关功能。
