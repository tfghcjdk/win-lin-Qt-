# NEV-SmartHMI 产品需求文档

> **当前状态更新（0.2.0-ui）**：已完成 Qt Widgets 页面与模拟交互，入口为 `NEV-SmartHMI.pro`，不再是辅助工程。详见 [UI 实现说明](doc/UI_IMPLEMENTATION.md)。下文“未实现/框架阶段”描述初始需求基线；本次已实现的 S1 显示和交互以实现说明逐项为准，S2 真实设备集成、S3 ARM 验证仍未完成。

> 文档状态：需求基线与框架规划；项目功能尚未实现。  
> 整理日期：2026-09-08。依据：[原始需求附件](.md)。

## 1. 项目目标与本阶段范围

NEV-SmartHMI 面向 X6818 开发板的新能源汽车智能仪表与车载终端，计划在 7 英寸、1024 × 600 触控屏上展示车辆、驾驶、导航、空调、媒体及行程信息。Windows 端用于后续界面开发和模拟数据验证，ARM Linux 端用于最终集成和板端验证。

本阶段只建立目录、源码与资源占位、构建入口和 Markdown 文档，另提供用户单独要求的 Windows Git 提交推送批处理脚本。十个 HMI 功能模块均为待开发需求，当前没有可运行的 HMI 程序、真实车辆连接、媒体播放或硬件控制。

原附件中的 Python 代码、HTML/PDF 生成示例和“已经生成”的文字作为原始资料保留；本阶段不执行这些示例，也不据此认定相关输出实际存在。Python 日志同步、ARM 构建、部署及 CAN 仿真脚本仅保留位置，未实现自动生成变更日志或自动修复缺陷状态。

## 2. 平台与界面约束

| 项目 | 需求基线 | 验收与确认边界 |
| --- | --- | --- |
| 目标硬件 | Samsung S5P6818 / X6818，ARM Cortex-A53 | 以实际开发板和厂商 SDK 为准；处理器名称不能直接确定目标程序 ABI |
| 目标系统 | Linux `3.4.39-9tripod`，原始系统信息为 `armv7l` | 需确认实际用户态、交叉编译器、sysroot 与库 ABI；不得直接按 aarch64 配置 |
| 目标 Qt | Qt 5.4.1，ARM 交叉编译版本 | 所有最终使用的 Qt API 和模块均需在该版本及实际板端构建中存在 |
| 开发环境 | Windows 10/11，Qt 5.14.2，MSVC 2017 32-bit | Windows 编译通过不能替代 Qt 5.4.1 板端兼容性验证 |
| 编程语言 | C++11 | 不使用 C++14/17 或 Qt 5.4.1 之后才提供的 API |
| 显示与输入 | 7 英寸 LCD，1024 × 600，触控 | 固定目标分辨率；内容不能超出屏幕，文字、卡片、按钮应完整可见 |
| 界面方案 | Qt Widgets、`QStackedWidget` 页面切换、必要处 `QPainter` 绘制 | 暗色车载主题；按可用区域分配布局，避免卡片固定高度相加导致溢出 |

原始内核信息包含 `SMP PREEMPT`，这不足以证明硬实时能力；本项目尚未确认实时性指标及其测试方法。

## 3. 信息布局与导航

主窗口预留顶部状态栏、左侧导航栏、中央仪表与卡片区域、下方行程与保养信息。导航入口包括：首页、车辆状态、导航、空调、媒体、设置。导航选中状态应与当前页面一致，并提供触摸反馈。

首页计划展示核心驾驶仪表、续航与胎压、能量流、空调、导航提示、倒车预览和媒体卡片。卡片和独立页面的复用方式在 UI 开发时确定，不能假定每张卡片都需要独立服务或新的架构层。设置页的具体选项未给出，当前只预留入口。

原型中的时间、速度、SOC、胎压、温度、歌曲、导航道路和里程均为展示示例，不是实时数据、启动默认状态或设备配置。未接入数据时，后续界面应明确标识模拟、未知、不可用或失联状态，不能直接显示“车辆状态正常”。

## 4. 十个核心功能模块

以下各项状态均为 **待实现**。示例仅用于理解原型布局。

### F01 顶部状态栏（Header Bar）

- **显示内容**：日期、星期与时间；4G、Wi-Fi、蓝牙、GPS 状态；SOC 与充电状态；平台标识。原型中的“2024年5月18日 14:36”“正在充电 80%”为示例。
- **目标行为**：展示系统时间和连接状态，按数据更新电量及充电标记。NTP/RTC 同步通过系统管理边界处理。
- **依赖与待确认**：时区、时间来源、同步权限、网络与定位状态获取方式、刷新周期；GPS 连接状态和定位有效性应分别定义。
- **功能验收**：状态输入变化可正确更新显示；断开或无数据状态不误报连接正常；时间格式与约定一致，状态栏在 1024 × 600 下不遮挡内容。

### F02 左侧主导航栏（Side Navigation）

- **显示内容**：首页、车辆状态、导航、空调、媒体、设置入口及底部标语。
- **目标行为**：通过 `QStackedWidget` 切换页面，当前入口高亮；支持触摸反馈与适度切换效果。
- **依赖与待确认**：页面详细布局、动画范围、触摸目标尺寸、设置页内容。
- **功能验收**：每个入口均进入对应页面；重复点击不会叠加页面；页面和高亮保持一致，触摸交互不造成布局越界。

### F03 核心驾驶仪表（Speed & Gear）

- **显示内容**：车速、单位、READY 状态、经济/标准/运动模式，以及 P/R/N/D 档位。原型为 `60 km/h`、经济模式、D 档。
- **目标行为**：接收 CAN 或串口模拟来源的车速、档位和动力模式，突出当前状态。
- **依赖与待确认**：车辆信号协议、更新频率、数值范围、超时条件、非法档位处理。此处是状态显示需求，未授权 UI 直接执行换挡控制。
- **功能验收**：各合法状态映射正确；缺失、过期、非法数据具有可识别显示；不会把示例车速或 READY 当成真实状态。

### F04 续航与胎压监测（Battery & TPMS）

- **显示内容**：剩余续航、SOC、车体图示、左前/右前/左后/右后胎压及状态提示。原型示例为 `428 km`、`80%`、前轮 `2.5 bar`、后轮 `2.4 bar`。
- **目标行为**：展示续航和四轮压力，超出确认阈值时以红色及明确状态提示报警。
- **依赖与待确认**：续航由车辆提供还是本地估算；胎压上下限、迟滞、去抖和恢复规则。附件中的 `<2.0 bar` 只是举例，不是已确认的报警阈值。
- **功能验收**：四轮位置和单位正确；按确认规则测试正常、低压、高压、恢复及传感器缺失；续航数据来源可说明。

### F05 能量流与功耗（Energy Stream）

- **显示内容**：电机功率、瞬时百公里电耗、累计里程和能量流向。原型示例为 `35 kW`、`12.3 kWh/100km`、`5236 km`。
- **目标行为**：区分电池向电机供能和动能回收，动态表示能量方向；显示或计算功耗与里程。
- **依赖与待确认**：功率正负约定、驱动/回收判断、计算来源、采样窗口、停车或低速下百公里电耗展示规则、累计里程权威来源。
- **功能验收**：驱动、回收、停止、失联状态显示正确；零速计算不产生除零或无意义数值；里程不因页面切换丢失或重复累计。

### F06 车载空调控制（HVAC Control）

- **显示内容**：目标温度与加减按钮、Auto、A/C、前/后除雾、内循环及风量。原型温度为 `24.0 ℃`。
- **目标行为**：目标温度按 `0.5 ℃` 步进调节；操作经业务层转化为 CAN/串口控制请求，展示车辆实际反馈。
- **依赖与待确认**：可设温度范围、风量档位、模式互斥规则、控制报文、设备地址、应答和超时策略。
- **功能验收**：温度步进和已确认上下限正确；模式互斥符合协议；未应答或请求失败可区分，按钮点击不直接冒充设备已执行。

### F07 实时导航小组件（Mini Navigation）

- **显示内容**：转向箭头、下一道路、剩余转向距离、限速、剩余行程和预计到达时间。原型中的“前方 800 米进入科技大道”“12 公里 · 28 分钟”为示例。
- **目标行为**：提供轻量化逐向导航提示、距离更新与限速预警；附件提到的轻量地图视图作为后续需求。
- **依赖与待确认**：导航数据供应方、地图资源与授权、定位来源、路线和 ETA 来源、超速判断规则；当前没有确定地图 SDK。
- **功能验收**：导航指令、距离和 ETA 随输入更新；无路线、无定位、限速未知状态明确；仅在有效数据和已确认规则下产生超速提示。

### F08 倒车影像预览（Rear Camera）

- **显示内容**：摄像头预览、红黄绿辅助线及全屏入口。
- **目标行为**：计划从 Linux V4L2 获取画面，以 `QPainter` 叠加轨迹线；支持进入与退出全屏。附件提及 OpenCV 作为可能图像来源，当前未确定为依赖。
- **依赖与待确认**：设备节点、像素格式、分辨率、帧率、标定方式、转向角来源及档位触发策略；无转向角或标定数据不能宣称动态轨迹准确。
- **功能验收**：画面方向与比例正确；全屏切换及返回可用；断流有明显状态提示；辅助线与确认的静态/动态规则一致，不遮挡必要导航操作。

### F09 车载多媒体播放器（Media Player）

- **显示内容**：歌名、歌手、封面、进度、播放/暂停、上一曲、下一曲和音量；歌词显示作为附件提出的后续需求。
- **目标行为**：播放音频文件、切换曲目、拖动进度和调节音量。原型歌曲及 `02:17 / 04:36` 均为示例，不附带实际音频素材。
- **依赖与待确认**：Qt Multimedia 板端支持情况、音频后端与 ALSA 设备、格式/编解码器、音频与封面授权、歌词格式和存储位置。
- **功能验收**：确认支持的音频文件可播放；进度与实际状态一致；暂停、切歌、拖动及音量控制有效；文件缺失、格式不支持、音频设备不可用有明确反馈。

### F10 行车数据与保养诊断（Trip Info & Service）

- **显示内容**：平均电耗、本次里程、行驶时长、平均车速、剩余保养里程、电池/胎压健康及 DTC 状态。原型中的 `125.6 km`、`2h 18min`、`3280 km` 为示例。
- **目标行为**：统计单次行程，持久化必要数据，按保养规则计算剩余里程并展示诊断信息。SQLite/JSON 是候选存储方案，尚未选定。
- **依赖与待确认**：行程开始/结束与重置条件、平均值计算口径、累计里程来源、保养周期、DTC 定义、持久化频率和掉电恢复规则。
- **功能验收**：按已确认口径对照输入验证统计；重启可恢复约定数据；缺失故障信息不会显示正常；保养倒计时和归零/复位行为符合确认规则。

## 5. 共通质量要求

1. **兼容性**：后续代码使用 C++11 和 Qt 5.4.1 可用 API，并分别验证 Windows 与 ARM 构建；硬件实现不进入通用 UI 依赖。
2. **界面适配**：1024 × 600 下无卡片溢出、文字遮挡或不可触达操作；板端验证中文字体、图标、触摸与实际 DPI。
3. **状态可信**：数值应具有来源、单位、有效性与更新时间；模拟数据明确标识，断开连接不能保留容易误判的正常状态。
4. **交互与线程**：UI 主线程不执行阻塞设备读取、同步网络等待或耗时解码；刷新频率、响应时限和资源预算需在板端测试后确认。
5. **控制反馈**：涉及硬件的请求和反馈分离，失败或超时可见；实际控制协议确认前只规划边界。
6. **资源可交付**：字体、图片、图标、地图和音频的来源与授权需记录；当前资源目录只有占位，不代表已获素材。

## 6. 分阶段验收

| 阶段 | 可验收交付 | 当前状态 |
| --- | --- | --- |
| S0 框架与文档 | 原附件目录映射、主文档、分层职责、源码与资源占位、CMake/qmake 框架入口、Windows Git 脚本 | 本次交付范围；以实际文件及验证结果为准 |
| S1 界面与模拟数据 | 十模块显示、页面导航、数据状态、1024 × 600 布局及 Windows 模拟验证 | 未实现 |
| S2 协议与设备集成 | 协议解析、车辆状态、空调应答、摄像头、音频、系统状态与持久化 | 未实现，依赖协议和硬件资料 |
| S3 ARM 板端验证 | Qt 5.4.1 交叉构建、部署、触摸、字体、画面、恢复行为及资源使用验证 | 未实现，依赖已确认工具链和板端环境 |

本阶段的成功标准是框架可阅读、可继续开发，文档能明确区分已建结构、未实现功能和待确认条件。CMake 配置或框架目标构建成功，不等于 HMI 编译完成、可运行或已通过十模块验收。

## 7. 已确定与待确认边界

**已确定**：项目路径、十模块范围、Qt 版本双环境、C++11、1024 × 600、分层目录、Widgets 技术方向，以及独立 Windows Git 提交推送脚本需求。

**待确认后才能实现相关集成**：目标 ABI 和 SDK、CAN/串口协议、传感器与报警阈值、控制应答规则、NTP/RTC 和连接信息来源、摄像头能力、音频后端、导航服务、图像与字体资源、数据统计/保养规则、性能与刷新指标。

技术接口和目录职责见 [TECH_SPEC.md](TECH_SPEC.md)，操作与框架入口见 [README.md](README.md)。需求变更记入 [CHANGELOG.md](CHANGELOG.md)，实际发现的问题记入 [BUG_TRACKER.md](BUG_TRACKER.md)，不能仅因提交说明包含 `fix` 就认定问题已验证解决。
