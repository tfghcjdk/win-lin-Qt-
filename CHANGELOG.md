# 版本变更日志

仅记录实际完成的变更。未实现的功能放入 PRD 或阶段计划，不计作已完成。

## [0.2.9-ui] - 2026-09-11

### 修改

- 将一次性 NTP / RTC 写入调整为定期校时：启动时从 RTC 恢复，联网且距离上次成功校时达到 6 小时后更新系统时间与 RTC。
- 断网或 NTP 请求失败时保留现有时间，每 10 分钟重新检查；同步状态保存在 `/Kd1234/config/rtc_time_sync.state`。
- 兼容旧 `.rtc_time_synced_once` 标记，升级后会按其时间判断首次定期校时。
- 修正ARM交叉编译的OpenCV查找逻辑，禁止缺少sysroot时回退到Ubuntu主机 `/usr/include`，并在缺少匹配的OpenCV 3.4.16开发头文件时给出明确的qmake错误。
- 增加独立的Qt HTTPS检测程序，直接验证板端QtNetwork、OpenSSL动态库、CA证书和高德Web服务Key，不依赖BusyBox `wget` 调用外部 `openssl` 命令。

## [0.2.8-ui] - 2026-09-11

### 新增

- 增加 Linux 开发板一次性 NTP 校时：直接发送标准 NTP UDP 请求，不依赖板端安装 `ntpd`。
- 系统时间设置成功后将 UTC 写入 `/dev/rtc0`、`/dev/rtc1` 或 `/dev/rtc`，两步均成功才创建 `/Kd1234/config/.rtc_time_synced_once`，后续启动不再写 RTC。
- 后续启动只读取已经同步的 RTC 来恢复系统时间，并将校时结果记录到 `/Kd1234/config/time_sync.log`。

### 修改

- 顶部北京时间增加秒钟显示，继续每秒刷新并固定使用 UTC+8。

## [0.2.7-ui] - 2026-09-11

### 新增

- 增加兼容 OpenCV 3.4.16 的单目地面测距模块，在摄像头线程中完成去畸变、地面 ROI 轮廓筛选和接地点距离换算。
- 增加 `/etc/nev-smarthmi/camera_calibration.ini` 标定配置读取与安全校验，未标定时禁止输出自动距离警告。
- Linux 构建链接开发板 `/usr/lib` 中的 `opencv_core`、`opencv_imgproc` 和 `opencv_calib3d`，并支持交叉编译 sysroot。

### 修改

- OpenCV 距离通过现有车辆状态模型驱动倒车障碍物警告；80 cm 内报警，连续三次未检出后清除。

## [0.2.6-ui] - 2026-09-11

### 修改

- 倒车辅助线从分段直线梯形调整为左右严格对称的平滑曲线轨迹。
- 保留绿、黄、红三段横向距离线，并加入圆角连接和半透明深色描边，提高复杂画面下的辨识度。
- 增加距离输入驱动的后方障碍物警告和设置页预览入口；真实雷达数据与车型阈值仍待接入和标定。

## [0.2.5-ui] - 2026-09-10

### 修改

- 主项目倒车摄像头默认设备统一为 `/dev/video9`。
- 采集参数从 640 × 480 / 15 FPS 提升为优先请求 1280 × 720 / 30 FPS，驱动仍可返回其支持的最近模式。
- 倒车画面改用 `Qt::SmoothTransformation` 保持比例缩放，并将显示区域调整为 16:9。
- 增加摄像头默认设备、分辨率和帧率配置测试。

## [0.2.4-ui] - 2026-09-10

### 新增

- 基于已验证的摄像头代码接入 Linux V4L2，支持 MJPEG/YUYV、MMAP、采集线程与断连/超时处理。
- 进入倒车影像时自动打开摄像头，返回或按 Esc 时自动停止并释放设备。
- 实时画面水平镜像，并保留绿、黄、红三段倒车辅助线。

### 修改

- 更新 Qt Designer 表单中的倒车影像说明，不增加手动打开/关闭按钮。

## [0.2.3-ui] - 2026-09-10

### 修改

- 将 Qt Designer 表单移动到 `src/ui/main_window.ui`，同步北京时间与温度滑条属性并加入 qmake `FORMS`。
- 修复车辆图片手工轮廓裁剪造成的车身和轮胎缺失，改用独立完整车图并保持比例缩放。

## [0.2.2-ui] - 2026-09-10

### 修改

- 顶部时钟改为每秒刷新的 UTC+8 北京时间，不再依赖电脑当前时区。
- 移除 UI 中所有“演示”字样，页脚保留“未连接车辆”状态提示。
- 空调卡片中间滑条改为温度控制，范围 16.0–30.0 ℃、步进 0.5 ℃，与加减按钮和详情页双向同步。
- 增加北京时间跨日换算和温度滑条控制测试。

## [0.2.1-ui] - 2026-09-10

### 清理

- 删除不被 `NEV-SmartHMI` 引用的旧 `Qt1604.pro`、根目录 `main.cpp` / `mainwindow.*`、空 `resources.qrc` 和旧 `images/` 占位目录。
- 删除旧工程的 `.build-ui/` 构建与验证产物、旧 Qt Creator 用户配置及 Designer 自动恢复文件。
- 将 `.build-ui/` 加入忽略规则，更新项目入口、构建和目录说明。
- 保留当前 `NEV-SmartHMI` 源码、原始需求、文档、测试、本机构建结果和当前 Qt Creator 用户配置。

## [0.2.0-ui] - 2026-09-08

### 新增

- 根据原型 JPG、`.md` 与 PDF 实现可运行的 1024 × 600 Qt Widgets 智能座舱。
- 实现六个导航页面、十模块首页、空调共享状态、媒体计时与控制、地图演示和倒车全屏预览。
- 新增数据断连和低胎压演示场景、Qt Test 交互测试及截图检查。
- qmake 从 aux 切换为 app；CMake 切换为 Qt Widgets 应用目标；加入嵌入式资源和 QSS。

### 边界

- 车辆/地图/媒体/摄像头为演示数据和静态素材，未接入真实硬件或音频输出。
- 本机使用 Qt 5.14.2 MinGW 32-bit；CMake、MSVC 和 ARM 构建尚未验证。
- 此版本实现时暂时保留了远端旧 Qt1604 工程；它随后在 0.2.1 中清理。新 UI 入口始终为 NEV-SmartHMI.pro。

## [0.1.0-skeleton] - 2026-09-08

### 新增

- 按原始 `.md` 建立 NEV-SmartHMI 分层目录及 C++ / QSS 占位文件。
- 建立 README、PRD、TECH_SPEC、BUG_TRACKER 与项目启动、协作、构建、目录说明文档。
- 提供 CMake 源文件浏览目标和 qmake 辅助工程入口，当前不编译可执行程序。
- 提供 Windows Git 自动提交推送批处理及操作说明，使用指定 SSH 私钥和 `origin/main`。
- 增加 Git 忽略和文本换行规则，排除本地构建产物、临时文件与常见私钥文件。

### 当前限制

- HMI 功能、硬件驱动、ARM 自动构建与部署、CAN 仿真尚未实现。
- Python 提交工具与 CHANGELOG / BUG_TRACKER 自动联动尚未实现；当前人工维护记录。
- 没有进行真实 GitHub 推送、Qt 应用编译或开发板运行验证。

## GitHub 仓库关联 - 2026-09-08

- 将本地 `main` 关联到 `git@github.com:tfghcjdk/win-lin-Qt-.git` 的 `origin/main`。
- 通过指定 SSH 私钥验证远端读取权限，获取并保留远端已有的 `ec828ec` 提交及原有 Qt1604 工程文件。
- 在已有历史基础上追加当前框架与文档；未修改远端原有功能代码，未执行强制推送或删除旧工程。
- 上方框架阶段的“没有进行真实 GitHub 推送”是初次交付时的验证范围记录；仓库关联属于后续独立操作。

## 后续记录格式

按版本新增章节，填写日期，并按新增、修改、修复分类。缺陷修复引用对应 Bug ID 与验证证据；发布版本号由实际开发进度确定。
