# NEV-SmartHMI 技术规范与架构说明

> 状态：框架阶段技术约束，尚未实现 HMI 功能。  
> 整理日期：2026-09-08。需求来源：[原始附件](.md)；功能基线：[PRD.md](PRD.md)。

## 1. 范围与当前构建状态

本阶段建立 Qt 项目目录、职责说明、头文件/源文件占位、资源与脚本预留位置。占位文件不提供 `QApplication` 入口、可运行界面、设备驱动或业务逻辑。

- `CMakeLists.txt` 使用 `LANGUAGES NONE` 与自定义源码展示目标，只服务于框架组织；此阶段不查找 Qt、不编译 C++、不生成 HMI 可执行文件。
- `NEV-SmartHMI.pro` 使用 `TEMPLATE = aux`，用于 qmake/Qt Creator 浏览框架；此阶段不构建应用程序。
- 未来开始功能开发后，再配置 C++11、Qt 模块、MOC/RCC、源文件编译及 ARM 工具链。框架配置成功不能替代编译、链接或板端运行验证。
- 根目录 `git_commit_push.bat` 是本次独立实现的 Windows Git 工具；`scripts/git_commit.py`、`scripts/build_arm.sh`、`scripts/deploy_to_board.sh`、`tests/mock_can_publisher.py` 仅为占位，尚无日志自动同步、交叉编译、部署或仿真功能。

## 2. 环境基线与兼容规则

| 环境 | 约束 | 开始功能开发前的检查 |
| --- | --- | --- |
| Windows 宿主 | Windows 10/11，Qt 5.14.2，MSVC 2017 32-bit | Qt Kit、编译器、Qt 模块和位数匹配；使用 C++11 兼容语法 |
| 目标硬件 | S5P6818 / X6818，ARM Cortex-A53，7 英寸触控屏 | 确认板卡、内存、显示和输入设备实际配置 |
| 目标系统 | `Linux X6818 3.4.39-9tripod ... SMP PREEMPT ... armv7l GNU/Linux` | 确认用户态 ABI、libc、动态链接器和内核设备能力 |
| 目标 Qt | Qt 5.4.1 ARM 构建 | 确认 Qt 安装前缀、平台插件、头文件、库、qmake mkspec 和已构建模块 |
| 工具链 | 与板端一致的 ARM Linux GCC 交叉工具链 | 编译器路径、target triple、sysroot、浮点 ABI、GCC/libstdc++ 版本及 SDK 来源待提供 |
| 显示 | 1024 × 600，目标固定分辨率 | 确认平台插件、DPI、触摸映射及中文字体覆盖 |

CPU 支持的指令架构与目标用户态 ABI 是不同条件。附件显示 `armv7l`，不能因 Cortex-A53 型号直接选择 aarch64 工具链；实际编译配置需要检查 SDK、Qt 库和目标可执行文件后确定。内核 `PREEMPT` 标记不自动意味着硬实时，本项目没有已验证的硬实时承诺。

### 2.1 C++ 与 Qt 使用约束

1. 采用 C++11，不使用 C++14/17 才提供的语法或标准库功能。
2. 以 Qt 5.4.1 为 API 下限；即使 Windows 的 Qt 5.14.2 可编译，也不使用后续 Qt 版本独有的类、方法或控件。遇到重载信号连接，选择 Qt 5.4.1 可用的显式函数指针写法，不依赖新版 `QOverload` 辅助接口。
3. UI 以 Qt Widgets 为规划方案，页面切换使用 `QStackedWidget`，仪表、能量流及轨迹叠加按需使用 `QPainter`。本框架不引入 QML 或新版 Qt Quick Controls 依赖。
4. Linux 特有的 SocketCAN、V4L2、ALSA 和系统调用留在硬件/平台实现边界内；不能在通用 UI 头文件中暴露 Linux 设备结构。
5. 所需 Qt SerialPort、Qt Multimedia、Qt SQL/驱动和其他模块须检查实际 Qt 5.4.1 构建是否包含；列入需求不等于目标镜像已经支持。
6. 源码采用 UTF-8，Windows 编译器字符集设置与板端字体加载在正式编译阶段配置并验证。文件路径不写死开发机 Qt 或 ARM SDK 的未知位置。

## 3. 三层结构与依赖方向

```text
src/ui/       Qt Widgets 展示、页面切换、触摸操作、图形绘制
       ↓ 用户意图                ↑ 已校验数据、状态与结果
src/core/     车辆数据中心、系统状态、媒体/导航等业务协调
       ↓ 控制请求                ↑ 设备帧、连接状态、错误
src/hardware/ CAN、串口、摄像头及后续平台音频适配
       ↓
Windows 模拟数据源 / X6818 Linux 设备

src/common/   公共数据约定、主题管理（供上述层按需使用）
```

UI 不直接读写设备；硬件层不依赖具体 Widget。Qt 信号/槽承担后续状态通知与操作请求连接，跨线程通信使用队列传递，具体类型和信号签名在协议与数据需求确认后定义。当前没有已实现的线程、协议解析器或服务。

该结构沿用附件的展示层、业务层和硬件层，不增加分布式服务、插件框架或额外基础设施。

## 4. 目录与模块职责

| 路径/规划组件 | 后续职责 | 本阶段边界 |
| --- | --- | --- |
| `src/main.cpp` | 应用启动、平台配置、对象连接、主窗口展示 | 仅占位，不含程序入口实现 |
| `src/ui/main_window.h/.cpp` | 1024 × 600 主窗口、顶栏、导航栏和页面容器 | 不创建可运行窗口 |
| `src/ui/header_bar/` | F01 时间、通信、定位、SOC 与充电状态显示 | 不执行网络或时间同步 |
| `src/ui/side_nav/` | F02 导航按钮、选中状态与页面切换请求 | 不管理硬件状态 |
| `src/ui/dashboard/` | F03 核心仪表、F04 续航/TPMS、F05 能量流与功耗 | 三个需求模块共用仪表区域，职责须分别保留 |
| `src/ui/hvac/` | F06 温度与模式交互、请求与反馈显示 | 不直接发送裸 CAN/串口报文 |
| `src/ui/navigation/` | F07 路口、限速、距离与 ETA 展示 | 不假定已有地图或路线服务 |
| `src/ui/reverse_camera/` | F08 画面展示、辅助线叠加、全屏切换 | 不承担 V4L2 采集操作 |
| `src/ui/media/` | F09 曲目信息、进度和播放控制界面 | 不假定已安装解码器或音频后端 |
| `src/ui/trip_info/` | F10 行程、保养和诊断信息展示 | 不直接决定统计或诊断规则 |
| `src/core/vehicle_data_center.h/.cpp` | 集中管理车速、档位、SOC、胎压等状态与更新通知；协调控制请求 | 附件计划单例模式；生命周期和线程归属在实现时明确，当前没有实例 |
| `src/core/system_manager.h/.cpp` | 系统时间、网络状态和设置管理 | 系统权限及平台数据来源待确认 |
| `src/core/` 后续媒体/导航职责 | 媒体状态协调、导航提示来源与业务转换 | 对应附件 `MediaManager` / `NaviService`；名称属于规划，未实现接口或后端 |
| `src/hardware/can_bus.h/.cpp` | Linux SocketCAN 传输、连接状态和错误反馈 | 报文定义、速率和设备名待确认 |
| `src/hardware/serial_port.h/.cpp` | 串口打开、配置、收发和错误反馈 | UART/RS485 使用范围、串口参数和帧格式待确认 |
| `src/hardware/camera_v4l2.h/.cpp` | Linux V4L2 采集、帧与设备状态输出 | 设备能力、缓冲和帧格式待确认 |
| `src/common/vehicle_types.h` | 平台无关的车辆数据、单位、有效性与状态约定 | 不提前填入未知 CAN ID、比例系数或阈值 |
| `src/common/theme_manager.h/.cpp` | 主题、样式与资源加载协调 | 当前无主题功能 |
| `assets/images/`、`assets/fonts/`、`assets/qss/` | 图片图标、中文字体、暗色 QSS | 资源与授权未交付；只有框架占位 |
| `doc/` | 环境、架构、协作、构建部署与文档说明 | 原附件的 HTML/PDF 文件名属于规划，不代表已生成 |
| `scripts/`、`tests/` | 后续自动化、交叉构建、部署与仿真验证 | 除根目录 Windows 批处理外，不执行功能性自动化 |

## 5. 数据与接口边界

下表确定层间职责，未冻结 C++ ABI、信号签名或协议字段。正式定义需与设备协议和功能验收同步评审。

| 边界 | 已确定的信息类别与方向 | 待确认内容 |
| --- | --- | --- |
| 设备 → 数据中心 | 车速、档位、READY、模式、SOC、充电、续航、四轮胎压、功率和里程 | CAN ID、字节序、DLC、缩放、偏移、枚举、校验、时间戳、超时和数据速率 |
| 数据中心 → UI | 带单位、来源、有效性与更新时间的业务状态 | 刷新频率、精度、状态类型、批量更新策略和信号签名 |
| UI → 空调业务 → 设备 | 温度、A/C、Auto、除雾、循环、风量请求 | 目标范围、模式约束、序号、应答关联、超时、重试和失败展示 |
| 系统管理 → 顶栏/设置 | 时间、网络、蓝牙、GPS 与系统信息 | NTP/RTC 配置、系统服务接口、权限及状态语义 |
| 导航来源 → 业务 → UI | 转向、道路、距离、限速、剩余行程与 ETA | 数据供应方、协议、定位有效性、地图许可和预警阈值 |
| 摄像头 → 影像 UI | 图像帧、帧时间、采集与断流状态 | 格式、分辨率、帧率、缓冲所有权、线程及失联策略 |
| 转向/标定数据 → 辅助线 | 静态或动态轨迹绘制所需数据 | 摄像头标定、车辆几何、转向角来源与换算规则 |
| 媒体业务 ↔ 平台后端 | 播放请求、曲目、时长、位置、音量、状态与错误 | QMediaPlayer/ALSA 等后端选择、格式支持及歌词/封面来源 |
| 行程业务 ↔ 持久化 | 行程统计、保养记录及诊断状态 | SQLite/JSON 选择、数据版本、写入时机、掉电恢复与数据保留范围 |

### 5.1 数据可信性与控制确认

- 原型数值不作为设备状态初始化依据，模拟来源应有明确标志。
- “没有故障数据”不能映射为“车辆正常”；缺失、过期、断流、解析失败应与有效数据分开表达。
- 胎压及超速报警必须等待阈值、迟滞和恢复规则确认；不把原型示例转换成生产配置。
- 控制请求、设备应答和实际状态分别建模，避免 UI 点击立即被当成硬件执行成功。
- 驾驶模式与档位当前属于展示需求，不能扩大为换挡或动力控制接口。

## 6. 线程、绘制与资源约束

Qt Widgets 及绘制操作归属 UI 线程。CAN、串口、摄像头采集和耗时音频/导航处理应与 UI 刷新隔离，通过队列传递结果。具体工作线程数量、缓冲大小和丢帧策略尚未定义，依据真实设备速率及板端测量确定，避免先搭建无需求支撑的复杂线程框架。

界面基于 1024 × 600 可用区域划分卡片，顶栏和左侧导航占用空间需计入预算。禁止仅按原型示例卡片尺寸叠加而使总高度超出屏幕。后续采用布局管理、合理间距和最小尺寸；仪表与辅助线按所属绘制区域计算坐标。无边框/全屏的具体平台行为需板端验证。

字体应覆盖中文、数字和所需符号；图标优先使用已确认的图片或矢量资源，不能依赖目标系统一定安装彩色 Emoji 字体。资源打包方案在实际资源到位后配置。刷新率、摄像头帧率、启动时限、CPU/内存占用目前没有实测指标，不在文档中填入假定达标数值。

## 7. 构建与部署演进

### 7.1 当前框架

项目可作为源码和文档目录打开，CMake/qmake 入口仅组织占位文件。即使运行其配置/构建命令成功，也只证明框架描述可用；它们不会提供 `NEV-SmartHMI.exe` 或 ARM 可执行程序。

### 7.2 Windows 功能开发阶段

确认 Qt 5.14.2 的 MSVC 2017 32-bit Kit 后，按模块逐步引入 `main.cpp`、Qt Core/Gui/Widgets 和模拟数据源。开启 C++11，配置 MOC/RCC 与字符集，使用 1024 × 600 窗口验证布局、导航、触摸替代输入、中文和状态更新。可选 Qt 模块按实际功能增量加入。

### 7.3 ARM 集成阶段

收集并记录 SDK、编译器、sysroot、Qt 5.4.1 路径和板端库清单，核对 ABI 后再编写工具链配置与交叉构建脚本。将硬件源文件限定在目标平台，Windows 使用独立模拟来源。部署前确认目标地址、用户名、目标目录、启动方式和显示/音频/设备权限；当前不预填未知主机、凭据或路径。

`scripts/build_arm.sh` 与 `scripts/deploy_to_board.sh` 当前仅占位，因此本阶段没有一键交叉编译或部署能力。

## 8. 验证计划与验收含义

| 验证层级 | 后续检查内容 | 当前阶段可以证明的内容 |
| --- | --- | --- |
| 框架检查 | 目录、文件、职责文档和相互引用；占位清晰 | 结构已建立，可继续开发 |
| 构建入口检查 | CMake 配置、自定义目标；有可用 Qt Kit 时检查 qmake aux | 不证明 C++ 编译、Qt 链接或应用运行 |
| 模拟功能验证 | 十模块状态、页面切换、异常数据、温度步进、控制应答及统计边界 | 尚未进行功能验证，模拟脚本未实现 |
| Windows 编译/运行 | C++11、所选 Qt 模块、布局与资源 | 当前无功能代码，未达到该阶段 |
| ARM 编译/运行 | ABI、Qt 5.4.1 API、设备访问、触摸/字体、性能及恢复行为 | 依赖真实 SDK、设备和接口资料，尚未实现 |

功能代码完成后优先测试有业务风险的边界：数据超时、非法值、零速功耗、报警恢复、空调应答失败、摄像头断流、音频缺失、行程持久化和保养复位。不能只验证界面示例值渲染，也不能把注释占位视为测试通过。

## 9. 协作与版本记录

后续每次模块开发应提供：开发目标、Qt 5.4.1/C++11/ARM 平台约束、1024 × 600 中的布局区域、已确认协议或模拟输入、预期行为与验收条件。头文件与源文件分离，必要注释解释业务规则与硬件限制，风格放入对应主题资源。需求变化先更新 PRD 和相关接口说明，再记录实现与验证结果。

变更记入 [CHANGELOG.md](CHANGELOG.md)，问题记入 [BUG_TRACKER.md](BUG_TRACKER.md)。文档应区分“已实现”“已验证”“待确认”；只有经过验证的修复才能标为已解决。附件所述 Python 自动追加日志是后续规划，当前维护文档采用手动记录。

Windows 提交推送工具位于 `git_commit_push.bat`，按用户指定项目路径、SSH 私钥路径及 `origin/main` 工作流处理提交与推送。其准确使用方式、退出条件和错误处理以脚本及项目操作说明为准。创建脚本不等于已经执行提交、远程认证或推送，不能把 Git 操作结果写成未发生的发布记录。
