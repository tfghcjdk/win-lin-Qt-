```python
import os
import weasyprint

# Define output file paths
html_path = "NEV_Smart_HMI_Project_Spec.html"
pdf_path = "NEV_Smart_HMI_Project_Spec.pdf"

html_content = """<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<title>新能源汽车智能终端 HMI 开发规范与启动文档</title>
<style>
    @page {
        size: A4;
        margin: 15mm 12mm;
        background-color: #0b132b;
    }
    
    *, *::before, *::after {
        box-sizing: border-box;
    }

    body {
        margin: 0;
        padding: 0;
        font-family: 'PingFang SC', 'Helvetica Neue', 'Microsoft YaHei', sans-serif;
        color: #e2e8f0;
        background-color: #0b132b;
        font-size: 10pt;
        line-height: 1.5;
    }

    /* Header Banner */
    .header-banner {
        background: linear-gradient(135deg, #1c2d5a 0%, #0c1838 100%);
        border: 1px solid #1e3a8a;
        border-radius: 8px;
        padding: 20px;
        margin-bottom: 20px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.3);
    }

    .header-title {
        font-size: 20pt;
        font-weight: bold;
        color: #38bdf8;
        margin: 0 0 8px 0;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .header-subtitle {
        font-size: 11pt;
        color: #94a3b8;
        margin: 0;
    }

    .meta-grid {
        margin-top: 15px;
        border-top: 1px solid #1e293b;
        padding-top: 10px;
        font-size: 9pt;
        color: #cbd5e1;
    }

    .meta-grid table {
        width: 100%;
        border-collapse: collapse;
    }

    .meta-grid td {
        padding: 4px 8px;
        vertical-align: top;
    }

    .meta-label {
        color: #38bdf8;
        font-weight: bold;
        width: 15%;
    }

    /* Section Styling */
    h2 {
        font-size: 14pt;
        color: #38bdf8;
        border-left: 4px solid #0284c7;
        padding-left: 10px;
        margin-top: 22px;
        margin-bottom: 12px;
        page-break-after: avoid;
    }

    h3 {
        font-size: 11pt;
        color: #f1f5f9;
        margin-top: 14px;
        margin-bottom: 8px;
        page-break-after: avoid;
    }

    p {
        margin-top: 0;
        margin-bottom: 10px;
        text-align: justify;
    }

    /* Cards / Boxes */
    .card {
        background-color: #111c38;
        border: 1px solid #1e293b;
        border-radius: 6px;
        padding: 12px 16px;
        margin-bottom: 14px;
        page-break-inside: avoid;
    }

    .card-title {
        font-weight: bold;
        color: #38bdf8;
        margin-bottom: 6px;
        font-size: 10.5pt;
    }

    /* Tables */
    table.data-table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 14px;
        font-size: 9pt;
        page-break-inside: avoid;
    }

    table.data-table th {
        background-color: #1e293b;
        color: #38bdf8;
        text-align: left;
        padding: 8px;
        border: 1px solid #334155;
        font-weight: bold;
    }

    table.data-table td {
        padding: 7px 8px;
        border: 1px solid #1e293b;
        background-color: #0f172a;
        color: #cbd5e1;
    }

    table.data-table tr:nth-child(even) td {
        background-color: #131f37;
    }

    /* Code Blocks */
    pre, code {
        font-family: 'Courier New', Courier, monospace;
    }

    pre {
        background-color: #080d1a;
        border: 1px solid #1e293b;
        border-radius: 5px;
        padding: 10px 12px;
        font-size: 8.5pt;
        color: #a5f3fc;
        overflow-x: auto;
        white-space: pre-wrap;
        word-wrap: break-word;
        margin-top: 6px;
        margin-bottom: 12px;
        page-break-inside: avoid;
    }

    /* Status Tags */
    .badge {
        display: inline-block;
        padding: 2px 6px;
        border-radius: 4px;
        font-size: 8pt;
        font-weight: bold;
    }
    .badge-primary { background-color: #0284c7; color: #ffffff; }
    .badge-success { background-color: #059669; color: #ffffff; }
    .badge-warning { background-color: #d97706; color: #ffffff; }

    ul, ol {
        margin-top: 0;
        margin-bottom: 10px;
        padding-left: 20px;
    }

    li {
        margin-bottom: 4px;
    }

    .highlight {
        color: #38bdf8;
        font-weight: bold;
    }
</style>
</head>
<body>

<div class="header-banner">
    <div class="header-title">新能源汽车智能仪表与车载终端 (NEV-SmartHMI)</div>
    <div class="header-subtitle">软件需求规格说明 (PRD)、系统架构设计与 AI 协作开发指南</div>
    <div class="meta-grid">
        <table>
            <tr>
                <td class="meta-label">目标平台:</td>
                <td>ARM Cortex-A53 (S5P6818 / X6818)</td>
                <td class="meta-label">屏幕规格:</td>
                <td>7寸 LCD (1024 × 600)</td>
            </tr>
            <tr>
                <td class="meta-label">宿主环境:</td>
                <td>Windows Qt 5.14.2 (MSVC 2017 32-bit)</td>
                <td class="meta-label">交叉编译链:</td>
                <td>ARM Linux GCC / Qt 5.4.1 (Linux 3.4.39)</td>
            </tr>
            <tr>
                <td class="meta-label">版本管理:</td>
                <td>Git + 自动化日志同步脚本</td>
                <td class="meta-label">项目周期:</td>
                <td>2026年 智能车载系统迭代</td>
            </tr>
        </table>
    </div>
</div>

<h2>一、 项目环境与硬件架构</h2>
<div class="card">
    <div class="card-title">1.1 硬件与系统约束</div>
    <p>项目运行于基于 <strong>Samsung S5P6818 (X6818)</strong> 八核 ARM Cortex-A53 架构的嵌入式开发板上，配备 7 英寸 1024×600 触控显示屏。系统运行 Linux 3.4.39 实时内核。</p>
    <table class="data-table">
        <tr>
            <th>环境分类</th>
            <th>详细参数 / 工具链</th>
            <th>说明及注意事项</th>
        </tr>
        <tr>
            <td><strong>目标开发板硬件</strong></td>
            <td>ARM Cortex-A53 (X6818芯片) / 7寸 LCD (1024x600)</td>
            <td>必须精准适配 1024x600 分辨率，UI组件不使用绝对高度撑满，防布局溢出。</td>
        </tr>
        <tr>
            <td><strong>开发板操作系统</strong></td>
            <td>Linux X6818 3.4.39-9tripod armv7l GNU/Linux</td>
            <td>嵌入式 Linux，内核较早，避免依赖过于新颖的 Linux 系统 API。</td>
        </tr>
        <tr>
            <td><strong>目标端 Qt 库</strong></td>
            <td>Qt 5.4.1 (ARM Cross-Compiled)</td>
            <td>编译链接目标版本，<strong>禁止使用 Qt 5.6+ 或 Qt 5.12+ 独有的 QML/C++ API</strong>。</td>
        </tr>
        <tr>
            <td><strong>PC端模拟开发环境</strong></td>
            <td>Windows 10/11 + Qt 5.14.2 (MSVC 2017 32-bit)</td>
            <td>PC 上运行模拟器进行快速 UI 与逻辑验证，保持跨平台编译兼容性。</td>
        </tr>
    </table>
</div>

<h2>二、 UI 原型功能总结与需求分析 (PRD)</h2>
<p>根据 HMI 界面原型设计图，系统分为顶部状态栏、左侧主导航 Dock、核心仪表盘、以及中部/下部功能卡片矩阵。</p>

<table class="data-table">
    <thead>
        <tr>
            <th style="width: 20%;">模块名称</th>
            <th style="width: 35%;">原型组件与显示内容</th>
            <th style="width: 45%;">功能与业务逻辑描述</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td><strong>1. 顶部状态栏<br>(Header Bar)</strong></td>
            <td>
                • 时间日期: 2024年5月18日 星期六 14:36<br>
                • 图标状态: 4G信号、Wi-Fi、蓝牙、GPS<br>
                • 充电状态: ⚡ 正在充电 80%<br>
                • 编译与平台标识: C++ / Qt | X6818 ARM
            </td>
            <td>
                • 系统时间软硬件同步（NTP/RTC）；<br>
                • 实时监测网络（4G/Wi-Fi/BT）与 GPS 连接；<br>
                • 实时刷新 SOC 电池电量与充电状态标志位。
            </td>
        </tr>
        <tr>
            <td><strong>2. 左侧导航栏<br>(Side Navigation)</strong></td>
            <td>
                • 首页、车辆状态、导航、空调、媒体、设置<br>
                • 底部 Slogan: 探索电动出行的更多可能
            </td>
            <td>
                • 采用 QStackedWidget 或 QML StackView 进行页面无缝切换；<br>
                • 具有高亮选中状态响应，支持触摸反馈。
            </td>
        </tr>
        <tr>
            <td><strong>3. 核心驾驶仪表<br>(Speed & Gear)</strong></td>
            <td>
                • 车速: 60 km/h (大字号)<br>
                • 状态: READY 经济模式<br>
                • 档位: P R N [D] (当前为D档)
            </td>
            <td>
                • 从 CAN 总线或串口模拟器接收实时车速与档位数据；<br>
                • 经济/运动/标准模式切换显示。
            </td>
        </tr>
        <tr>
            <td><strong>4. 续航与胎压监测<br>(Battery & TPMS)</strong></td>
            <td>
                • 剩余续航: 428 km / 电量 80%<br>
                • 3D/2D 车体图形 + 4轮胎压 (左前/右前 2.5 bar, 左后/右后 2.4 bar)<br>
                • 状态提示: 车辆状态正常
            </td>
            <td>
                • 实时显示 4 个轮胎的压力数值（bar）；<br>
                • 异常高压/低压自动触发亮红警报。
            </td>
        </tr>
        <tr>
            <td><strong>5. 能量流与功耗<br>(Energy Stream)</strong></td>
            <td>
                • 电机功率: 35 kW<br>
                • 瞬间电耗: 12.3 kWh/100km<br>
                • 累计里程: 5236 km
            </td>
            <td>
                • 动态绘制能量流动方向（电池->电机 / 回馈充电）；<br>
                • 实时计算与显示瞬时功耗及总里程数。
            </td>
        </tr>
        <tr>
            <td><strong>6. 空调控制卡片<br>(HVAC Control)</strong></td>
            <td>
                • 温度调节: 24.0 ℃ (- / +按钮)<br>
                • 模式: 自动 (Auto)、A/C、前挡除雾、后挡除雾、内循环
            </td>
            <td>
                • 目标温度调节与按键响应；<br>
                • A/C 开关、循环模式切换及风量逻辑控制。
            </td>
        </tr>
        <tr>
            <td><strong>7. 导航提示卡片<br>(Mini Navigation)</strong></td>
            <td>
                • 路口指引: 前方 800 米 进入 科技大道<br>
                • 限速标志: 60 km/h<br>
                • 行程预测: 12 公里 · 28 分钟 | 预计 15:04 到达
            </td>
            <td>
                • 转向箭头提示与距离倒计时；<br>
                • 限速超速预警提醒；地图轻量化视图渲染。
            </td>
        </tr>
        <tr>
            <td><strong>8. 倒车影像卡片<br>(Rear Camera)</strong></td>
            <td>
                • 倒车摄像头视频画面预览<br>
                • 动态/静态倒车轨迹辅助线<br>
                • "点击进入 >" 全屏入口
            </td>
            <td>
                • 通过 V4L2 采集摄像头 Video 帧数据或 OpenCV 图像流；<br>
                • 实时叠加 QPainter 绘制的轨迹线；点击可全屏。
            </td>
        </tr>
        <tr>
            <td><strong>9. 车载多媒体卡片<br>(Media Player)</strong></td>
            <td>
                • 歌曲: 向着更远的地方 - 追光者<br>
                • 进度条: 02:17 / 04:36<br>
                • 控制组件: 上一曲、播放/暂停、下一曲
            </td>
            <td>
                • 基于 QMediaPlayer / Linux ALSA 音频播放；<br>
                • 歌词解析显示、进度条拖拽与控制播放。
            </td>
        </tr>
        <tr>
            <td><strong>10. 行车数据与保养<br>(Trip Info & Service)</strong></td>
            <td>
                • 平均电耗: 13.2 kWh/100km | 本次里程: 125.6 km<br>
                • 行驶时长: 2h 18min | 平均车速: 54 km/h<br>
                • 维护提醒: 下次保养还剩 3280 km | 动力电池/胎压正常
            </td>
            <td>
                • 单次行程数据统计与持久化存储（SQLite/JSON）；<br>
                • 保养里程倒计时与系统健康诊断。
            </td>
        </tr>
    </tbody>
</table>

<h2>三、 系统技术架构与选型</h2>
<div class="card">
    <div class="card-title">3.1 架构层次设计</div>
    <pre>
+-----------------------------------------------------------------------+
|                       应用展示层 (Qt 5.4.1 UI / QML)                   |
|  [HeaderBar] [SideNav] [Instrument] [HVAC] [Navi] [Media] [TPMS/Trip] |
+-----------------------------------------------------------------------+
                                    | (Qt Signals / Slots / Q_PROPERTY)
+-----------------------------------------------------------------------+
|                       业务逻辑与数据中心 (C++ Core)                   |
|  [VehicleDataCenter]  [SystemManager]  [MediaManager]  [NaviService]  |
+-----------------------------------------------------------------------+
                                    | (Thread Queue / Protocol Parser)
+-----------------------------------------------------------------------+
|                     硬件抽象与通信层 (Hardware Drivers)               |
|   [CAN Bus (SocketCAN)]  [Serial/UART]  [V4L2 Camera]  [ALSA Audio]   |
+-----------------------------------------------------------------------+
                                    |
+-----------------------------------------------------------------------+
|                 ARM Cortex-A53 (X6818) Linux 3.4.39 内核              |
+-----------------------------------------------------------------------+
    </pre>
</div>

<h2>四、 ChatGPT 协作开发规程与项目管理</h2>
<p>为了保证与 ChatGPT 协作效率高、逻辑严密，并确保跨平台（Qt 5.14.2 PC / Qt 5.4.1 ARM）顺利编译，制定以下 Prompt 开发规范：</p>

<div class="card">
    <div class="card-title">4.1 与 ChatGPT 交互的标准 Prompt 范本</div>
    <pre>
【开发目标】: 实现 HMI 空调控制模块 (HVACControlWidget)
【硬件与环境】: ARM Cortex-A53 X6818, Linux 3.4.39, Qt 5.4.1 (C++11, 无 C++14/17 语法, 禁止 Qt 5.6+ 独有 API)。
【分辨率要求】: 1024x600，固定无边框窗口。
【功能需求】: 
 1. 包含温度显示 (24.0℃)，增加/减少按钮 (0.5℃步进)。
 2. 包含 Auto、A/C、前/后除雾、内循环按键。
 3. 发送对应信号至 VehicleDataCenter 单例。
【代码要求】:
 - 头文件与源文件分离 (hvac_widget.h / hvac_widget.cpp)。
 - 包含详细代码注释，提供 Qt Widget 样式表 (QSS) 适配暗色调车载风格。
    </pre>
</div>

<h2>五、 自动化 Git 提交与 Changelog / Bug_Tracker 工作流</h2>
<p>为满足自动同步 `CHANGELOG.md` 与 `BUG_TRACKER.md` 的管理需求，项目配备了自动化提交脚本 <code>scripts/git_commit.py</code>。</p>

<div class="card">
    <div class="card-title">5.1 自动化工作流说明</div>
    <ul>
        <li><strong>功能迭代 (feat):</strong> 当提交类型选 <code>feat</code> 时，脚本会自动提取 commit message 追加写入 <code>CHANGELOG.md</code>，带上当前版本号与时间戳。</li>
        <li><strong>修复Bug (fix):</strong> 当提交类型选 <code>fix</code> 时，脚本提示输入 Bug ID 与描述，自动记录至 <code>BUG_TRACKER.md</code> 列表中，并将状态标记为 <span class="badge badge-success">RESOLVED</span>。</li>
        <li><strong>自动 Push:</strong> 完成文档自动追加后，脚本自动执行 <code>git add .</code>, <code>git commit</code> 和 <code>git push origin main</code>。</li>
    </ul>
</div>

<h3>5.2 自动化脚本源码 (scripts/git_commit.py)</h3>
<pre>
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
import os
import sys
import datetime
import subprocess

def run_cmd(cmd):
    result = subprocess.run(cmd, shell=True, text=True, capture_output=True)
    if result.returncode != 0:
        print(f"Error executing: {cmd}\\n{result.stderr}")
        return False
    return True

def update_changelog(commit_msg):
    date_str = datetime.datetime.now().strftime("%Y-%m-%d %H:%M")
    entry = f"\\n- [{date_str}] {commit_msg}"
    with open("CHANGELOG.md", "a", encoding="utf-8") as f:
        f.write(entry)
    print("Updated CHANGELOG.md")

def update_bug_tracker(bug_id, bug_desc, fix_desc):
    date_str = datetime.datetime.now().strftime("%Y-%m-%d")
    entry = f"| {bug_id} | {date_str} | {bug_desc} | {fix_desc} | 已解决 (Resolved) |\\n"
    with open("BUG_TRACKER.md", "a", encoding="utf-8") as f:
        f.write(entry)
    print("Updated BUG_TRACKER.md")

def main():
    print("=== NEV-SmartHMI 自动化 Git 提交与日志更新工具 ===")
    commit_type = input("选择提交类型 (feat/fix/docs/refactor/style): ").strip().lower()
    msg = input("输入 Commit 提交说明: ").strip()
    
    if not msg:
        print("提交信息不能为空！")
        return

    full_commit_msg = f"{commit_type}: {msg}"

    if commit_type == "feat":
        update_changelog(msg)
    elif commit_type == "fix":
        bug_id = input("输入 Bug 编号 (例: BUG-001): ").strip()
        bug_desc = input("输入 Bug 简述: ").strip()
        update_bug_tracker(bug_id, bug_desc, msg)
        update_changelog(f"[Fix {bug_id}] {msg}")

    # 执行 Git 操作
    print("正在提交至 Git 仓库...")
    if run_cmd("git add .") and run_cmd(f'git commit -m "{full_commit_msg}"'):
        push = input("是否立即推送到远程仓库? (y/n): ").strip().lower()
        if push == 'y':
            if run_cmd("git push origin main"):
                print("成功推送到 GitHub 远程仓库！")
            else:
                print("Git push 失败，请检查网络或权限。")
        else:
            print("提交已完成在本地仓库。")

if __name__ == "__main__":
    main()
</pre>

<h2>六、 项目目录树设计</h2>
<p>为满足 Qt 5.4.1 ARM 交叉编译与跨平台模块化开发，规划目录结构如下：</p>
<pre>
NEV-SmartHMI/
├── CMakeLists.txt                  # CMake 自动化构建脚本 (适配 Qt 5.4 / 5.14)
├── NEV-SmartHMI.pro                # qmake 工程文件 (备用)
├── README.md                       # 项目总体介绍
├── PRD.md                          # 产品需求文档
├── TECH_SPEC.md                    # 技术规范与架构说明
├── BUG_TRACKER.md                  # Bug 跟踪记录表
├── CHANGELOG.md                    # 版本变更日志
├── doc/                            # 各种说明文档及生成的 PDF
│   ├── NEV_Smart_HMI_Project_Spec.pdf
│   └── NEV_Smart_HMI_Project_Spec.html
├── scripts/                        # 自动化与交叉编译脚本
│   ├── git_commit.py               # Git 提交与日志自动同步脚本
│   ├── build_arm.sh                # ARM Qt 5.4.1 一键交叉编译脚本
│   └── deploy_to_board.sh          # SCP 传输应用程序至 X6818 开发板脚本
├── src/                            # 源代码目录
│   ├── main.cpp                    # 程序主入口
│   ├── core/                       # 核心业务逻辑与数据中心
│   │   ├── vehicle_data_center.h/.cpp # 全局车载数据总线 (单例)
│   │   └── system_manager.h/.cpp    # 系统设置与网络状态管理
│   ├── hardware/                   # 硬件通信驱动层
│   │   ├── can_bus.h/.cpp          # CAN总线通信接口 (SocketCAN)
│   │   ├── serial_port.h/.cpp      # 串口通信接口
│   │   └── camera_v4l2.h/.cpp      # 倒车摄像头 V4L2 视频流采集
│   ├── ui/                         # UI 视图层组件
│   │   ├── main_window.h/.cpp      # 主窗口 (1024x600 容器)
│   │   ├── header_bar/             # 顶栏状态条组件
│   │   ├── side_nav/               # 左侧导航栏组件
│   │   ├── dashboard/              # 仪表盘/速度/档位/TPMS组件
│   │   ├── hvac/                   # 空调控制组件
│   │   ├── navigation/             # 导航卡片组件
│   │   ├── reverse_camera/         # 倒车影像与轨迹线绘制组件
│   │   ├── media/                  # 多媒体播放器组件
│   │   └── trip_info/              # 行车信息与维护提醒组件
│   └── common/                     # 通用定义与主题 QSS
│       ├── theme_manager.h/.cpp    # 界面主题/QSS管理
│       └── vehicle_types.h         # 数据结构体定义
├── assets/                         # 资源文件
│   ├── images/                     # 汽车图示、卡片背景、图标
│   ├── fonts/                      # 嵌入式矢量字体
│   └── qss/                        # 样式表文件
└── tests/                          # 模拟测试与数据仿真
    └── mock_can_publisher.py       # CAN 报文数据模拟发送脚本
</pre>

</body>
</html>
"""

# Write HTML file
with open(html_path, "w", encoding="utf-8") as f:
    f.write(html_content)

print(f"HTML generated at: {html_path}")

# Render PDF file using WeasyPrint
weasyprint.HTML(filename=html_path).write_pdf(pdf_path)
print(f"PDF generated at: {pdf_path}")


```

```text
HTML generated at: NEV_Smart_HMI_Project_Spec.html
PDF generated at: NEV_Smart_HMI_Project_Spec.pdf


```

Your PDF and HTML startup documents are ready:


---

### 一、 硬件与开发环境分析

根据你提供的开发板与环境信息，项目环境配置如下：

* **目标开发板硬件**：ARM Cortex-A53 架构（基于 Samsung S5P6818 / X6818 芯片）。
* **显示屏规格**：7 英寸 LCD，分辨率 **1024 × 600**（属于标准车载中控屏宽高比，UI 需精准适配此绝对分辨率）。
* **目标板操作系统**：`Linux X6818 3.4.39-9tripod #9 SMP PREEMPT Thu Jul 20 14:16:45 CST 2017 armv7l GNU/Linux`。
* **目标板交叉编译链 / Qt 版本**：**Qt 5.4.1** (ARM GCC Cross-Toolchain)。
* **Windows PC 开发环境**：**Qt 5.14.2 (MSVC 2017 32-bit)**。

#### ⚠️ 跨平台开发注意事项与兼容性规范：

1. **Qt 版本向下兼容控制**：由于 PC 上使用的是 Qt 5.14.2，而开发板使用的是 **Qt 5.4.1**，在 PC 端编写代码时**切勿使用 Qt 5.6 或 Qt 5.12+ 引入的新特性/新类**（例如：`QOverload` 语法、新版 QML 控件库等）。语法需遵循 **C++11** 标准，防止在开发板交叉编译时报错。
2. **分辨率固定与 DPI 缩放**：屏幕固定为 `1024x600`，主窗口可设置为无边框全屏展示（`Qt::FramelessWindowHint`），界面元素避免采用动态视口缩放，推荐采用标准 Grid/Card 布局或 QPainter 绘制。

---

### 二、 功能开发需求（基于原型图的功能拆解与描述）

根据你提供的 HMI 原型图，该软件为一款**新能源汽车智能中控仪表与车载服务程序 (NEV-SmartHMI)**，主要分为以下 **10 大核心功能模块**：

| 模块名称 | 界面位置 | 原型图呈现元素 | 详细功能与业务逻辑描述 |
| --- | --- | --- | --- |
| **1. 顶部状态栏** | 屏幕顶端 | 日期时间（2024年5月18日 14:36）、4G/Wi-Fi/蓝牙/GPS状态图标、充电状态（⚡ 正在充电 80%）、系统平台标识 | 实时显示系统时间（与 RTC/NTP 同步），监测车载网络与 GPS 信号强弱；实时接收电量 SOC 与充电状态；右侧展示运行环境参数。 |
| **2. 左侧主导航栏** | 屏幕最左侧 Dock | 首页、车辆状态、导航、空调、媒体、设置 图标及文字；底部 Slogan 标识 | 侧边栏按钮响应触摸点击，驱动主工作区进行 `QStackedWidget` 界面切换；具备当前选中态高亮及动画效果。 |
| **3. 核心驾驶仪表** | 中上方主显示区 | 车速（60 km/h）、模式（READY 经济模式）、档位（P R N **[D]**） | 显示当前实时车速与动力模式（经济/标准/运动）；清晰标识当前档位，D/R/N/P 切换高亮。 |
| **4. 续航与胎压监测** | 中上方中央卡片 | 剩余续航（428 km）、电池图标（80%）、3D/2D 车体矢量图、4 轮胎压（左前/右前 2.5bar，左后/右后 2.4bar）、状态提示（车辆状态正常） | 实时估算剩余续航里程；可视化监控 4 个轮胎的实时气压，若胎压过低（如 <2.0bar）或过高自动触发红框报警。 |
| **5. 能量流与功耗** | 中上方右侧卡片 | 电机功率（35 kW）、瞬间电耗（12.3 kWh/100km）、累计里程（5236 km）、电机能量流动示意图 | 动态展示动力电池与电机之间的能量流动方向（驱动/动能回收）；实时计算并刷新百公里电耗及总行驶里程。 |
| **6. 车载空调控制** | 中下方左一卡片 | 目标温度（24.0℃）与 +/- 调节、AUTO 自动模式、A/C、前/后挡风除雾、内循环开关、风量调节 | 触控调节空调温度（0.5℃ 步进）；切换 A/C 制冷、内外循环及除雾模式；发送串口/CAN 控制指令给车身控制器。 |
| **7. 实时导航小组件** | 中下方左二卡片 | 路口转向指引（前方 800 米进入科技大道）、限速标志（60）、剩余距离与时长（12 公里·28分钟）、预计到达时间（15:04） | 轻量化导航提示组件，显示实时 Turn-by-Turn 转向箭头、道路名称、限速预警及 ETA 剩余时间估算。 |
| **8. 倒车影像预览** | 中下方右二卡片 | 倒车摄像头实时画面、辅助倒车轨迹线（红/黄/绿）、"点击进入 >" | 采集 V4L2 摄像头视频帧进行预览；叠加 QPainter 绘制的响应式倒车辅助轨迹线；点击可全屏放大。 |
| **9. 车载多媒体播放器** | 中下方右一卡片 | 歌名/歌手（向着更远的地方 - 追光者）、播放进度条（02:17 / 04:36）、上一曲/暂停/下一曲控制 | 音频文件播放与进度控制，支持专辑封面显示、播放/暂停、切歌与音量调节。 |
| **10. 行车数据与保养诊断** | 底端左侧与中间 | 平均电耗（13.2 kWh/100km）、本次里程（125.6 km）、行驶时长（2h 18min）、平均车速（54 km/h）、下次保养倒计时（3280 km）、健康状态诊断 | 统计单次出步行车数据；记录保养剩余里程，实时监控动力电池与系统故障码（DTC）。 |

---

### 三、 项目管理与与 ChatGPT 协作开发规程

为了确保你让 ChatGPT（或 AI）进行高效代码开发，同时自动化管理项目的 **PRD**、**TECH_SPEC**、**BUG_TRACKER** 和 **CHANGELOG**，制定以下规程：

#### 1. 给 ChatGPT 发送的 Prompt 规范范本

每次让 ChatGPT 开发某个功能模块时，必须附带**硬件限制**，避免生成无法在 Qt 5.4.1 / ARM 开发板上运行的代码：

> **Prompt 示例：**
> “请为我编写 `NEV-SmartHMI` 项目中的**空调控制模块 (HVACWidget)** 代码。
> * **环境限制**：ARM Cortex-A53, Linux 3.4, Qt 5.4.1，纯 C++11，禁止使用 Qt 5.6+ 独有 API。
> * **屏幕分辨率**：适配 1024x600 布局中的一个卡片区域（宽 300px，高 220px）。
> * **功能需求**：根据原型图实现 24.0℃ 温度调节（+ / -）、Auto、A/C、除雾及内循环切换，并触发 Qt 信号将数据发送至 `VehicleDataCenter`。
> * **输出要求**：请分别提供 `hvac_widget.h`、`hvac_widget.cpp` 及对应的 QSS 样式。”
> 
> 

---

#### 2. 自动化提交与 Changelog / Bug_Tracker 日志更新脚本

在项目根目录下放置 Python 自动化提交脚本 `scripts/git_commit.py`。开发测试完成后，只需在终端运行该脚本，输入 commit 信息，脚本会自动：

1. 若为功能迭代 (`feat`)：自动追加记录到 `CHANGELOG.md`。
2. 若为 Bug 修复 (`fix`)：自动更新 `BUG_TRACKER.md` 并标记状态为已解决，同步记录到 `CHANGELOG.md`。
3. 自动执行 `git add .`、`git commit` 并提示推送 (`git push`) 到 GitHub 远程仓库。

##### 自动化提交脚本源码 (`scripts/git_commit.py`)：

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
NEV-SmartHMI 自动化 Git 提交与日志更新工具
用法: python scripts/git_commit.py
"""
import os
import sys
import datetime
import subprocess

def run_cmd(cmd):
    result = subprocess.run(cmd, shell=True, text=True, capture_output=True)
    if result.returncode != 0:
        print(f"执行失败: {cmd}\n{result.stderr}")
        return False
    return True

def update_changelog(commit_type, msg):
    date_str = datetime.datetime.now().strftime("%Y-%m-%d %H:%M")
    entry = f"\n- [{date_str}] [{commit_type.upper()}] {msg}"
    changelog_path = "CHANGELOG.md"
    if not os.path.exists(changelog_path):
        with open(changelog_path, "w", encoding="utf-8") as f:
            f.write("# 项目版本变更日志 (CHANGELOG)\n")
    with open(changelog_path, "a", encoding="utf-8") as f:
        f.write(entry)
    print("✅ 已自动更新 CHANGELOG.md")

def update_bug_tracker(bug_id, bug_desc, fix_desc):
    date_str = datetime.datetime.now().strftime("%Y-%m-%d")
    tracker_path = "BUG_TRACKER.md"
    if not os.path.exists(tracker_path):
        with open(tracker_path, "w", encoding="utf-8") as f:
            f.write("# Bug 追踪与修复记录表 (BUG_TRACKER)\n\n")
            f.write("| Bug编号 | 修复日期 | 问题描述 | 修复方案 / Commit | 状态 |\n")
            f.write("| :--- | :--- | :--- | :--- | :--- |\n")
    
    entry = f"| {bug_id} | {date_str} | {bug_desc} | {fix_desc} | 已解决 (Resolved) |\n"
    with open(tracker_path, "a", encoding="utf-8") as f:
        f.write(entry)
    print("✅ 已自动更新 BUG_TRACKER.md")

def main():
    print("===========================================")
    print("   NEV-SmartHMI 自动化提交工具")
    print("===========================================")
    print("请选择提交类型:")
    print("  1. feat     (新功能/组件迭代)")
    print("  2. fix      (修复问题/Bug)")
    print("  3. docs     (更新文档)")
    print("  4. refactor (代码重构/优化)")
    
    choice = input("请选择编号 (1-4, 默认1): ").strip()
    type_map = {"1": "feat", "2": "fix", "3": "docs", "4": "refactor"}
    commit_type = type_map.get(choice, "feat")

    msg = input("请输入 Commit 说明信息: ").strip()
    if not msg:
        print("❌ 错误：Commit 信息不能为空！")
        return

    full_commit_msg = f"{commit_type}: {msg}"

    if commit_type == "feat":
        update_changelog("feat", msg)
    elif commit_type == "fix":
        bug_id = input("请输入 Bug 编号 (例如 BUG-001): ").strip() or "BUG-UNCATEGORIZED"
        bug_desc = input("请输入 Bug 问题描述: ").strip() or msg
        update_bug_tracker(bug_id, bug_desc, msg)
        update_changelog("fix", f"[{bug_id}] {msg}")
    else:
        update_changelog(commit_type, msg)

    print("\n正在提交本地代码仓库...")
    if run_cmd("git add .") and run_cmd(f'git commit -m "{full_commit_msg}"'):
        print("✅ 本地 Git Commit 成功！")
        push_choice = input("是否立即 Push 推送到 GitHub 远程仓库? (y/n, 默认y): ").strip().lower()
        if push_choice in ["", "y", "yes"]:
            print("正在推送到 GitHub...")
            if run_cmd("git push origin main"):
                print("🚀 成功推送到 GitHub 远程仓库！")
            else:
                print("❌ Git push 失败，请检查网络或 Gitee/GitHub 权限。")
    else:
        print("❌ Commit 过程遇到错误，请检查 git 状态。")

if __name__ == "__main__":
    main()

```

---

### 四、 项目启动文档（PDF & HTML 已自动生成）

已通过后台 Python 环境为您自动渲染并生成了标准、精美的启动文档：

1. **HTML 版项目文档**：`NEV_Smart_HMI_Project_Spec.html`
2. **PDF 版项目启动文档**：`NEV_Smart_HMI_Project_Spec.pdf`

*包含完整环境参数、PRD需求、架构图、ChatGPT交互模板及 Git 提交脚本规范，可直接保存打印或用于项目开题说明。*

---

### 五、 设计项目规范目录结构

为支持 Qt 5.4.1 ARM 交叉编译与模块化敏捷开发，建议项目采用如下目录架构：

```text
NEV-SmartHMI/
├── CMakeLists.txt                  # CMake 交叉编译构建脚本 (适配 Qt 5.4 / 5.14)
├── NEV-SmartHMI.pro                # Qt Creator qmake 工程文件 (备用)
├── README.md                       # 项目简介与运行说明
├── PRD.md                          # 产品需求文档
├── TECH_SPEC.md                    # 技术规范与架构设计说明
├── BUG_TRACKER.md                  # Bug 追踪与修复记录表
├── CHANGELOG.md                    # 版本变更日志
├── doc/                            # 项目设计图与文档
│   ├── NEV_Smart_HMI_Project_Spec.pdf
│   └── NEV_Smart_HMI_Project_Spec.html
├── scripts/                        # 自动化与交叉编译工作流脚本
│   ├── git_commit.py               # 自动化 Git Commit 及文档同步脚本
│   ├── build_arm.sh                # ARM Qt 5.4.1 一键交叉编译脚本
│   └── deploy_to_board.sh          # 通过 SCP/TFTP 将可执行文件部署到 X6818 开发板
├── src/                            # C++ 源代码目录
│   ├── main.cpp                    # 程序主入口 (初始化QApplication, 设置全屏1024x600)
│   ├── core/                       # 核心业务逻辑与数据中心
│   │   ├── vehicle_data_center.h/.cpp # 全局车载数据总线 (单例模式, 管理车速/电量/胎压)
│   │   └── system_manager.h/.cpp    # 系统设置、网络状态与系统时间同步
│   ├── hardware/                   # 硬件驱动抽象层
│   │   ├── can_bus.h/.cpp          # SocketCAN 通信接口
│   │   ├── serial_port.h/.cpp      # UART/RS485 串口通信 Driver
│   │   └── camera_v4l2.h/.cpp      # V4L2 摄像头视频流采集 (倒车影像)
│   ├── ui/                         # UI 界面与 Widget 组件库
│   │   ├── main_window.h/.cpp      # 1024x600 主界面容器
│   │   ├── header_bar/             # 顶栏状态条组件
│   │   ├── side_nav/               # 左侧 Dock 导航栏组件
│   │   ├── dashboard/              # 核心仪表盘/车速/档位/TPMS 胎压卡片
│   │   ├── hvac/                   # 空调控制卡片组件
│   │   ├── navigation/             # 导航提示卡片组件
│   │   ├── reverse_camera/         # 倒车影像与 QPainter 动态轨迹线组件
│   │   ├── media/                  # 多媒体播放器卡片组件
│   │   └── trip_info/              # 行车信息与保养诊断卡片组件
│   └── common/                     # 通用数据结构与样式主题
│       ├── theme_manager.h/.cpp    # 暗色主题管理与 QSS 样式表加载器
│       └── vehicle_types.h         # 车载传感器数据结构体定义
├── assets/                         # 静态资源目录
│   ├── images/                     # 汽车 2D/3D 图示、状态图标、卡片背景
│   ├── fonts/                      # 矢量字体文件 (确保嵌入式 Linux 中文显示正常)
│   └── qss/                        # 界面深色车载主题样式表 (dark_theme.qss)
└── tests/                          # 测试与数据仿真
    └── mock_can_publisher.py       # Python 编写的 CAN 报文模拟发送工具 (用于调试)

```